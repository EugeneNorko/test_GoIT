import styled from "styled-components";
import StyledContainer from "../container/Container.styled";

const ContainerHeader = styled(StyledContainer)`
  min-height: 60px;
  display: flex;
  align-items: center;
`;

export default ContainerHeader;
